# keyd configuration — installed to /etc/keyd/default.conf by bootstrap.sh
# Reload after edits with:  sudo keyd reload
# See: https://github.com/rvaiya/keyd

[ids]
# Apply to every keyboard.
*

[main]
# Caps Lock becomes Escape (for vim / zsh vi-mode).
capslock = esc
